# ─── Supabase Configuration ───────────────────────────────
# Get these from: https://supabase.com/dashboard
#   → Your Project → Project Settings → API
#
# URL must start with https:// and end with .supabase.co
# Key is the "anon / public" key (NOT the service_role key)
#
# IMPORTANT: "Failed to fetch" errors mean these values are
# still placeholders or incorrect.
# ──────────────────────────────────────────────────────────

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ─── Seed Script Only ─────────────────────────────────────
# Required ONLY for: npm run seed:hc
# Get the "service_role" key (NOT the anon key) from:
#   Supabase Dashboard → Project Settings → API → service_role
# Delete this value after seeding for security.
# ──────────────────────────────────────────────────────────
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
